import { type Config } from "prettier";

const config: Config = {
  trailingComma: "all",
  singleQuote: true,
  semi: true,
  printWidth: 120,
  tabWidth: 2,
  useTabs: false,
};

export default config;